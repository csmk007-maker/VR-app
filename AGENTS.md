# VR App Instructions

- Follow the architectural guidelines mentioned in the prompt.
- Use `rajawali` library or standard OpenGL ES. Let's try `rajawali` first, but it is unmaintained. Since we need to write production-ready code, we should probably use standard OpenGL ES or an up-to-date modern library.
- Let's use `OpenGL ES 2.0/3.0` directly or standard libraries, rather than importing full blown game engines if possible.
